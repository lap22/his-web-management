# API base URL
VITE_API_URL=https://api.your-his.local

# Optional bearer token for local development only.
# Do not commit real secrets.
VITE_API_TOKEN=

# Set true to force local mock data even when an API URL is configured.
VITE_USE_MOCK_API=false
